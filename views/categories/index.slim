#categories
  h1 Categories
  - if @categories.empty?
    p There doesn't seem to be any categories, yet. 
    p How about you go and <a href="/categories/new">create</a> some.

  - for category in @categories
    .category
      .name
        h2
          a(href="categories/#{category.id}") = category.name
      .description
        = category.description
      .meta
        .topics-count
          .count = "#{category.topics.size} topics"
        .replies-count
          .count = "#{category.replies.count} replies"
      - if admin?
        .actions
          ul
            li
              a(href="/categories/#{category.id}/edit") Edit
            li
              a(href="/categories/#{category.id}/delete") Delete

  - if admin?
    .actions
      a(href="/categories/new") New Category

